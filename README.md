# Scratch Repository

This is a scratch repository for testing a local cloud agent.
GitHub API publishing was tested successfully.
The checkout also uses the GitHub API.
The workflow no longer uses Git subprocesses.
